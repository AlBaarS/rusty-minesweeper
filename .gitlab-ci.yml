image: rust:latest

stages:
  - test
  - build

unit-tests:
  stage: test
  script:
    - cargo install cargo-tarpaulin
    - cargo tarpaulin
  coverage: '/^\[0-9]+\.\[0-9]+/% coverage,*/'

building:
  stage: build
  script:
    - cargo build
