import getDataFromAPI from './functions/getDataFromAPI'
import getSeed from './functions/getSeed';

function DisplayBoard(seed: number) {
    if (seed == undefined) {
        const seed = getSeed();
    }
    
    // const raw_board
}

export default DisplayBoard;